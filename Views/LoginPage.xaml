<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:TripMate.ViewModels"
             x:Class="TripMate.Views.LoginPage"
             x:DataType="vm:LoginViewModel"
             Title="Login"
             Shell.NavBarIsVisible="False">

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="20" VerticalOptions="Center">
            <!-- App branding -->
            <Label Text="TripMate" FontSize="36" FontAttributes="Bold"
                   HorizontalOptions="Center" Margin="0,40,0,20"
                   TextColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource White}}" />
            <Label Text="Split trip expenses with friends"
                   HorizontalOptions="Center" Opacity="0.8"
                   Margin="0,0,0,40" />

            <Border Style="{StaticResource InputCardStyle}">
                <VerticalStackLayout Spacing="16">
                    <Entry Placeholder="Email" Text="{Binding Email}"
                           Keyboard="Email" ReturnType="Next" />
                    <Entry Placeholder="Password" Text="{Binding Password}"
                           IsPassword="True" ReturnType="Done"
                           ReturnCommand="{Binding LoginCommand}" />
                </VerticalStackLayout>
            </Border>

            <Label Text="{Binding StatusMessage}" IsVisible="{Binding HasError}"
                   TextColor="Red" HorizontalOptions="Center" />

            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}"
                              HorizontalOptions="Center" Color="{StaticResource Primary}" />

            <Button Text="Log In" Command="{Binding LoginCommand}"
                    HorizontalOptions="Fill" Margin="16,8" />

            <Button Text="Create Account" Command="{Binding GoToRegisterCommand}"
                    BackgroundColor="Transparent" TextColor="{StaticResource Primary}"
                    HorizontalOptions="Fill" Margin="16,0" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
